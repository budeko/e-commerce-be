import { FastifyReply, FastifyRequest } from 'fastify';

// TODO: seller approvalStatus === 'approved' kontrolü
export const requireApprovedSeller = async (
  _request: FastifyRequest,
  _reply: FastifyReply
) => {
  // TODO
};
