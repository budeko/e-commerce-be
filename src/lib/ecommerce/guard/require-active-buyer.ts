import { FastifyReply, FastifyRequest } from 'fastify';

// TODO: buyer isActive === true kontrolü
export const requireActiveBuyer = async (
  _request: FastifyRequest,
  _reply: FastifyReply
) => {
  // TODO
};
